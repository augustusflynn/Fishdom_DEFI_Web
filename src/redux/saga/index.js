function* mySaga() {
	// In the future, we may want to interfere when call an actions
	// For example: Handle call api when call actions
	// Handles folder will contain all function you want to handle for each case of action
	// Requests folder for call api using in function in handlers folder
}

export default mySaga;
